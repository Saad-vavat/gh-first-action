name : First_workFlow
on : workflow_dispatch
jobs:
  first-job:
    runs-on: 	windows-latest
    steps:
      - name: Print greeting
        run : echo "Hello World!"
      - name : Print goodBye
        run : echo "Done Bye"
